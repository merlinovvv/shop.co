import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["i.imgur.com", "api.escuelajs.co", "i.pinimg.com", "placeimg.com", "becute.com.pl"],
  },
};

export default nextConfig;
